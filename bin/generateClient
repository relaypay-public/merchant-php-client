#!/bin/bash
cd deps
composer install --no-dev --optimize-autoloader --ignore-platform-reqs
cd ..
swagger-codegen generate -i deps/vendor/relaypay/merchant-api-spec/openapi.yml -l php --c './client-config.json'
cd client
composer install --no-dev --optimize-autoloader --ignore-platform-reqs